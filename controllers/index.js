const users = require('./user');
const rounds = require('./round');
const questions = require('./questController');
const scores = require('./scoreController');

module.exports = {
    users,
    rounds,
    questions,
    scores
}